# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: ../ui/vod.spec.ts >> VOD / Search >> TC-VOD-005: sort Newest ↔ Oldest
- Location: ui/vod.spec.ts:151:7

# Error details

```
TimeoutError: locator.click: Timeout 25000ms exceeded.
Call log:
  - waiting for getByRole('button', { name: /sort:/i })
    - locator resolved to <button type="button" aria-expanded="false" aria-haspopup="listbox" data-testid="sort-select" class="flex w-full items-center gap-2 font-body transition-colors h-10 rounded-ds-lg border border-border-default bg-surface-input px-3 text-sm text-white hover:border-border-strong">…</button>
  - attempting click action
    2 × waiting for element to be visible, enabled and stable
      - element is visible, enabled and stable
      - scrolling into view if needed
      - done scrolling
      - <div class="absolute inset-0 bg-gradient-to-t from-black/60 via-transparent to-transparent"></div> from <div class="fixed inset-0 z-50 flex items-center justify-center">…</div> subtree intercepts pointer events
    - retrying click action
    - waiting 20ms
    - waiting for element to be visible, enabled and stable
    - element is visible, enabled and stable
    - scrolling into view if needed
    - done scrolling
    - <div class="absolute inset-0 bg-gradient-to-t from-black/60 via-transparent to-transparent"></div> from <div class="fixed inset-0 z-50 flex items-center justify-center">…</div> subtree intercepts pointer events
  - retrying click action
    - waiting 100ms
    - waiting for element to be visible, enabled and stable
    - element is visible, enabled and stable
    - scrolling into view if needed
    - done scrolling
    - <div class="grid grid-cols-1 lg:grid-cols-2 gap-6 sm:gap-8 md:gap-12 lg:gap-16 items-center">…</div> from <div class="fixed inset-0 z-50 flex items-center justify-center">…</div> subtree intercepts pointer events
  - retrying click action
    - waiting 100ms
    - found getByRole('button', { name: /maybe later, continue free/i }), intercepting action to run the handler
    - locator handler has finished, waiting for getByRole('button', { name: /maybe later, continue free/i }) to be hidden
    37 × locator resolved to visible <button data-slot="button" data-testid="subscribe-now-dismiss" class="inline-flex items-center justify-center gap-2 whitespace-nowrap font-medium disabled:cursor-not-allowed disabled:opacity-50 [&_svg]:pointer-events-none [&_svg:not([class*='size-'])]:size-4 shrink-0 [&_svg]:shrink-0 outline-none aria-invalid:ring-destructive hover:cursor-pointer border-none bg-transparent h-12 rounded-md px-6 has-[>svg]:px-5 w-full text-gray-400 hover:text-white py-3 sm:py-4 lg:py-5 text-xs sm:text-sm hover:bg-white/…>Maybe later, continue free</button>

```

# Test source

```ts
  1   | import { test, expect, type Browser, type Page } from '@playwright/test';
  2   | import { requireEnv, resolveWebBaseUrl, setSessionCookie } from '../utils/auth';
  3   | import {
  4   |   dismissBanner,
  5   |   dismissCookieConsentBarIfPresent,
  6   |   installSubscribeModalAutoDismiss,
  7   | } from '../utils/ui-helpers';
  8   | 
  9   | /**
  10  |  * TC-VOD-001…008 — On Demand search (docs/Test Cases.md §5).
  11  |  *
  12  |  * REWRITTEN for the search redesign (web PR #194 / commit 096111e). The legacy
  13  |  * `/search` page (h1 "Discover events", a "Filters" button opening an "Advanced
  14  |  * filters" panel with an "Apply" button, sort options Newest/A-Z/Sport) was
  15  |  * replaced by the config-driven `EventSearch` engine:
  16  |  *   - h1 "Search the library", hero input placeholder "Just start to type …"
  17  |  *   - filters live in a desktop `FacetSidebar` (<aside lg:block>) and apply
  18  |  *     LIVE (no Apply button): Sport/Date chips are `button[aria-pressed]`,
  19  |  *     Content toggles are `button[role="checkbox"]`
  20  |  *   - sort is a DS Select in the results toolbar: trigger `button` named
  21  |  *     "Sort: <label>", options `role="option"` (Newest first / Oldest first /
  22  |  *     Name A–Z / Name Z–A / By sport)
  23  |  *   - results are `EventCard` links (`a[href*="/event/"]`); the no-match state
  24  |  *     is an `<h3>` "No titles found" / "No titles match those filters"
  25  |  *
  26  |  * Requires staging + REGULAR_TOKEN. The engine gates the first paint on a
  27  |  * branded loader until mounted + active-sports load, so we wait on the h1.
  28  |  */
  29  | 
  30  | const RESULT = 'a[href*="/event/"]';
  31  | 
  32  | async function openSearchPage(browser: Browser) {
  33  |   const webBase = resolveWebBaseUrl();
  34  |   const ctx = await browser.newContext();
  35  |   await setSessionCookie(ctx, requireEnv('REGULAR_TOKEN'), webBase);
  36  |   const page = await ctx.newPage();
  37  |   await installSubscribeModalAutoDismiss(page);
  38  |   await page.goto('/search', { waitUntil: 'domcontentloaded' });
  39  |   await dismissBanner(page);
  40  |   await dismissCookieConsentBarIfPresent(page);
  41  | 
  42  |   await expect(
  43  |     page.getByRole('heading', { level: 1, name: /search the library/i }),
  44  |   ).toBeVisible({ timeout: 45_000 });
  45  | 
  46  |   return { page, close: () => ctx.close() };
  47  | }
  48  | 
  49  | /** Engine has settled when either result cards or the no-match state is shown. */
  50  | async function expectResultsOrEmpty(page: Page, timeout = 30_000) {
  51  |   const results = page.locator(RESULT).first();
  52  |   const empty = page.getByRole('heading', { name: /no titles/i });
  53  |   await expect(results.or(empty).first()).toBeVisible({ timeout });
  54  | }
  55  | 
  56  | /** Change sort via the DS Select in the results toolbar (desktop). */
  57  | async function selectSort(page: Page, optionName: RegExp) {
> 58  |   await page.getByRole('button', { name: /sort:/i }).click();
      |                                                      ^ TimeoutError: locator.click: Timeout 25000ms exceeded.
  59  |   await page.getByRole('option', { name: optionName }).click();
  60  | }
  61  | 
  62  | test.describe('VOD / Search', () => {
  63  |   test('TC-VOD-001: typing in the search bar returns results (or empty state)', async ({
  64  |     browser,
  65  |   }) => {
  66  |     const { page, close } = await openSearchPage(browser);
  67  |     try {
  68  |       const search = page.getByPlaceholder(/just start to type/i);
  69  |       await search.click();
  70  |       await search.pressSequentially('rowing', { delay: 60 });
  71  |       // debounce (400ms) + API + grid paint
  72  |       await expectResultsOrEmpty(page, 35_000);
  73  |     } finally {
  74  |       await close();
  75  |     }
  76  |   });
  77  | 
  78  |   test('TC-VOD-002: selecting a sport facet filters the list live', async ({
  79  |     browser,
  80  |   }, testInfo) => {
  81  |     const { page, close } = await openSearchPage(browser);
  82  |     try {
  83  |       await expectResultsOrEmpty(page);
  84  | 
  85  |       // Sport + Date facets are the aria-pressed chips; the first is a sport.
  86  |       const firstChip = page.locator('aside button[aria-pressed]').first();
  87  |       if ((await firstChip.count()) === 0) {
  88  |         testInfo.skip(true, 'No sport facets rendered on this environment');
  89  |         return;
  90  |       }
  91  |       await firstChip.click();
  92  |       await expect(firstChip).toHaveAttribute('aria-pressed', 'true');
  93  | 
  94  |       // List re-queries live (no Apply) → results or empty state.
  95  |       await expectResultsOrEmpty(page);
  96  |     } finally {
  97  |       await close();
  98  |     }
  99  |   });
  100 | 
  101 |   test('TC-VOD-003: infinite scroll loads more (or stays when no more pages)', async ({
  102 |     browser,
  103 |   }, testInfo) => {
  104 |     const { page, close } = await openSearchPage(browser);
  105 |     try {
  106 |       const cards = page.locator(RESULT);
  107 |       await expectResultsOrEmpty(page);
  108 |       const initial = await cards.count();
  109 |       if (initial === 0) {
  110 |         testInfo.skip(true, 'No events to test pagination on this environment');
  111 |         return;
  112 |       }
  113 | 
  114 |       await cards.last().scrollIntoViewIfNeeded();
  115 |       await page.waitForTimeout(1500); // IntersectionObserver (rootMargin 1200px)
  116 |       const after = await cards.count();
  117 |       expect(after).toBeGreaterThanOrEqual(initial);
  118 |     } finally {
  119 |       await close();
  120 |     }
  121 |   });
  122 | 
  123 |   test('TC-VOD-004: opening an event runs the access check', async ({
  124 |     browser,
  125 |   }, testInfo) => {
  126 |     const { page, close } = await openSearchPage(browser);
  127 |     try {
  128 |       const first = page.locator(RESULT).first();
  129 |       if (!(await first.isVisible({ timeout: 30_000 }).catch(() => false))) {
  130 |         testInfo.skip(true, 'No events to open on this environment');
  131 |         return;
  132 |       }
  133 |       await first.click();
  134 | 
  135 |       const player = page.locator('video, [class*="video-js"]').first();
  136 |       const subGate = page
  137 |         .getByRole('heading', { name: /watch with subscription|subscribe/i })
  138 |         .or(page.getByText(/watch with subscription/i).first());
  139 |       const geo = page.getByText(/not available in your region/i).first();
  140 |       const notFound = page.getByRole('heading', { name: /offside|not found/i });
  141 |       const scheduled = page.getByRole('heading', { name: /upcoming|scheduled|delayed/i });
  142 | 
  143 |       await expect(
  144 |         player.or(subGate).or(geo).or(notFound).or(scheduled).first(),
  145 |       ).toBeVisible({ timeout: 45_000 });
  146 |     } finally {
  147 |       await close();
  148 |     }
  149 |   });
  150 | 
  151 |   test('TC-VOD-005: sort Newest ↔ Oldest', async ({ browser }) => {
  152 |     const { page, close } = await openSearchPage(browser);
  153 |     try {
  154 |       await expectResultsOrEmpty(page);
  155 |       await selectSort(page, /oldest/i);
  156 |       await expect(page.getByRole('button', { name: /sort:.*oldest/i })).toBeVisible();
  157 |       await expectResultsOrEmpty(page);
  158 | 
```