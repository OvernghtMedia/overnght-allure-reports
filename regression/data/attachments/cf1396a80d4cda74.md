# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: ../ui/home.spec.ts >> Home screen >> TC-HOME-003: navigate to Event Video Player from event card
- Location: ui/home.spec.ts:108:7

# Error details

```
Error: expect(page).toHaveURL(expected) failed

Expected pattern: /\/event\//
Received string:  ""
Timeout: 30000ms

Call log:
  - Expect "toHaveURL" with timeout 30000ms
    - found getByRole('button', { name: /maybe later, continue free/i }), intercepting action to run the handler
    - locator handler has finished, waiting for getByRole('button', { name: /maybe later, continue free/i }) to be hidden
    38 × locator resolved to visible <button data-slot="button" data-testid="subscribe-now-dismiss" class="inline-flex items-center justify-center gap-2 whitespace-nowrap font-medium disabled:cursor-not-allowed disabled:opacity-50 [&_svg]:pointer-events-none [&_svg:not([class*='size-'])]:size-4 shrink-0 [&_svg]:shrink-0 outline-none aria-invalid:ring-destructive hover:cursor-pointer border-none bg-transparent h-12 rounded-md px-6 has-[>svg]:px-5 w-full text-gray-400 hover:text-white py-3 sm:py-4 lg:py-5 text-xs sm:text-sm hover:bg-white/…>Maybe later, continue free</button>

```

# Test source

```ts
  20  |   // Spend the one-shot subscribe modal before any card/CTA clicks (see helper).
  21  |   await settleSubscribeModal(page);
  22  |   return {
  23  |     page,
  24  |     close: () => ctx.close(),
  25  |   };
  26  | }
  27  | 
  28  | test.describe('Home screen', () => {
  29  |   test('TC-HOME-001: all home screen sections', { tag: '@smoke' }, async ({ browser }) => {
  30  |     const { page, close } = await openAuthenticatedHome(browser);
  31  |     try {
  32  |       const liveNow = page.getByRole('heading', { name: 'Live Now' });
  33  |       const horizon = page.getByRole('heading', { name: 'On the Horizon' });
  34  |       await expect(liveNow.or(horizon).first()).toBeVisible({
  35  |         timeout: 90_000,
  36  |       });
  37  | 
  38  |       // Catalog rails are data-driven on staging — a rail only renders when it
  39  |       // has content (e.g. "Discover most popular sports" needs sports data).
  40  |       // Check the known rails opportunistically: a present rail must render
  41  |       // correctly, an absent one (no seed data) is not a failure here. Hero +
  42  |       // #main-content (above) assert the authenticated home rendered.
  43  |       const knownSectionTitles = [
  44  |         'Discover most popular sports',
  45  |         'Conferences',
  46  |         'Featured Past Events',
  47  |         'Featured Overnght Shows',
  48  |         'All Overnght Shows',
  49  |         'All Past Events',
  50  |       ] as const;
  51  | 
  52  |       for (const title of knownSectionTitles) {
  53  |         const h = page.getByRole('heading', { level: 2, name: title });
  54  |         if ((await h.count()) === 0) continue; // rail not present (no data) — skip
  55  |         await h.first().scrollIntoViewIfNeeded().catch(() => {}); // may re-render during scroll
  56  |         await expect(h.first()).toBeVisible({ timeout: 30_000 });
  57  |       }
  58  |     } finally {
  59  |       await close();
  60  |     }
  61  |   });
  62  | 
  63  |   test('TC-HOME-002: open a sport page from the home sports section', async ({
  64  |     browser,
  65  |   }, testInfo) => {
  66  |     // UPDATED for the header/nav redesign (web PR #194 / commit 1b3d5a7
  67  |     // "drop legacy header/footer/search"): the burger menu + ExploreSidebar
  68  |     // ("Explore" drawer with per-sport buttons) was removed. Sports are now
  69  |     // reached from the "Discover most popular sports" home section, which links
  70  |     // straight to `/sports/:slug`. We click the first such link instead of the
  71  |     // old burger → aside flow (which silently skipped after the removal).
  72  |     const { page, close } = await openAuthenticatedHome(browser);
  73  |     try {
  74  |       const sportLink = page.locator('a[href*="/sports/"]').first();
  75  | 
  76  |       // Scroll the home feed until a sport link mounts (lazy sections).
  77  |       for (let i = 0; i < 6; i++) {
  78  |         if (await sportLink.isVisible().catch(() => false)) break;
  79  |         await page.keyboard.press('End');
  80  |         await page.waitForTimeout(500);
  81  |       }
  82  | 
  83  |       if (!(await sportLink.isVisible().catch(() => false))) {
  84  |         testInfo.skip(true, 'No /sports/ link rendered on home — empty catalog?');
  85  |         return;
  86  |       }
  87  | 
  88  |       await sportLink.scrollIntoViewIfNeeded();
  89  |       await sportLink.click();
  90  |       await expect(page).toHaveURL(/\/sports\//, { timeout: 30_000 });
  91  |       await expect(
  92  |         page.getByRole('heading', { level: 1 }),
  93  |       ).toBeVisible({ timeout: 45_000 });
  94  | 
  95  |       const live = page.getByRole('heading', { name: 'Live Now' });
  96  |       const sched = page.getByRole('heading', { name: 'On the Horizon' });
  97  |       const past = page.getByRole('heading', { name: 'All Past Events' });
  98  |       const shows = page.getByRole('heading', { name: 'All Overnght Shows' });
  99  |       const anyH2 = page.getByRole('heading', { level: 2 }).first();
  100 |       await expect(live.or(sched).or(past).or(shows).or(anyH2).first()).toBeVisible({
  101 |         timeout: 60_000,
  102 |       });
  103 |     } finally {
  104 |       await close();
  105 |     }
  106 |   });
  107 | 
  108 |   test('TC-HOME-003: navigate to Event Video Player from event card', async ({
  109 |     browser,
  110 |   }) => {
  111 |     const { page, close } = await openAuthenticatedHome(browser);
  112 |     try {
  113 |       // Find first event card link and click it
  114 |       const eventLink = page.locator('a[href*="/event/"]').first();
  115 |       await eventLink.scrollIntoViewIfNeeded();
  116 |       await expect(eventLink).toBeVisible({ timeout: 60_000 });
  117 |       await eventLink.click();
  118 | 
  119 |       // Verify event page opened
> 120 |       await expect(page).toHaveURL(/\/event\//, { timeout: 30_000 });
      |                          ^ Error: expect(page).toHaveURL(expected) failed
  121 | 
  122 |       // Accept any valid event page state
  123 |       const player = page.locator('video, [class*="video-js"]').first();
  124 |       const subGate = page.getByRole('heading', { name: /watch with subscription|subscribe/i });
  125 |       const upcomingGate = page.getByRole('heading', { name: /upcoming|scheduled|delayed/i });
  126 |       const geoGate = page.getByRole('heading', { name: /content unavailable|unavailable in your region/i });
  127 |       const notFound = page.getByRole('heading', { name: /offside|not found/i });
  128 | 
  129 |       await expect(
  130 |         player.or(subGate).or(upcomingGate).or(geoGate).or(notFound).first(),
  131 |       ).toBeVisible({ timeout: 45_000 });
  132 |     } finally {
  133 |       await close();
  134 |     }
  135 |   });
  136 | 
  137 |   test('TC-HOME-004: navigate to VOD from "See More" on Home', async ({
  138 |     browser,
  139 |   }, testInfo) => {
  140 |     const { page, close } = await openAuthenticatedHome(browser);
  141 |     try {
  142 |       // Wait for content sections to load
  143 |       const anySection = page.getByRole('heading', { level: 2 }).first();
  144 |       await expect(anySection).toBeVisible({ timeout: 60_000 });
  145 | 
  146 |       // Scroll down to find "See more" / "See all" CTA
  147 |       // Home page has multiple sections, CTAs may be in EventSectionCta
  148 |       const seeMore = page
  149 |         .getByRole('link', { name: /see more|see all/i })
  150 |         .or(page.locator('a').filter({ hasText: /see more|see all/i }))
  151 |         .first();
  152 | 
  153 |       // Scroll through the page looking for the CTA
  154 |       for (let i = 0; i < 5; i++) {
  155 |         if (await seeMore.isVisible().catch(() => false)) break;
  156 |         await page.keyboard.press('End');
  157 |         await page.waitForTimeout(500);
  158 |       }
  159 | 
  160 |       if (!(await seeMore.isVisible().catch(() => false))) {
  161 |         testInfo.annotations.push({ type: 'note', description: '"See More" CTA not found — no content sections may be loaded' });
  162 |         // At least verify the home page loaded
  163 |         await expect(anySection).toBeVisible();
  164 |         return;
  165 |       }
  166 | 
  167 |       // The modal can arm late under load (slow hydration → its 3s timer starts
  168 |       // late), i.e. after the openHome settle and during the scroll above — spend
  169 |       // it again right before the click so it can't intercept the navigation.
  170 |       await settleSubscribeModal(page);
  171 |       await seeMore.scrollIntoViewIfNeeded();
  172 |       await seeMore.click();
  173 | 
  174 |       // Should navigate to search or an event list page
  175 |       await expect(page).toHaveURL(/\/search|\/event|\/sports/, {
  176 |         timeout: 20_000,
  177 |       });
  178 |     } finally {
  179 |       await close();
  180 |     }
  181 |   });
  182 | });
  183 | 
```