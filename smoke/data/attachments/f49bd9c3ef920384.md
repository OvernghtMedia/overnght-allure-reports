# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: ../ui/footer.spec.ts >> Footer (design system) >> TC-FOOTER-001: footer nav + Watch/Support links + legal bar
- Location: ui/footer.spec.ts:13:7

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByRole('navigation', { name: 'Footer' })
Expected: visible
Timeout: 20000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 20000ms
  - waiting for getByRole('navigation', { name: 'Footer' })

```

# Page snapshot

```yaml
- generic [ref=e3]:
  - img "Sorry, something went wrong" [ref=e5]
  - generic [ref=e6]:
    - heading "Critical Error" [level=1] [ref=e7]
    - paragraph [ref=e8]: We encountered a critical error. Please try reloading the page.
  - button "Reload page" [ref=e9] [cursor=pointer]
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | import { dismissCookieConsentBarIfPresent } from '../utils/ui-helpers';
  3  | 
  4  | /**
  5  |  * TC-FOOTER-* — Design-system site footer (web 97cad46/6add494). A `<footer>`
  6  |  * with `<nav aria-label="Footer">` (Watch / Account / Support columns) and a
  7  |  * legal bar `<nav aria-label="Legal">` (Privacy / Terms). Source of truth:
  8  |  * components/ds/footer/footerNav.ts. Token-free; the global footer renders on
  9  |  * content routes like /contact (home, /faq and /search own their own layout
  10 |  * without the global footer), so we assert it on /contact.
  11 |  */
  12 | test.describe('Footer (design system)', () => {
  13 |   test('TC-FOOTER-001: footer nav + Watch/Support links + legal bar', { tag: '@smoke' }, async ({
  14 |     page,
  15 |   }) => {
  16 |     await page.goto('/contact', { waitUntil: 'domcontentloaded' });
  17 |     await dismissCookieConsentBarIfPresent(page);
  18 | 
  19 |     const footerNav = page.getByRole('navigation', { name: 'Footer' });
  20 |     await footerNav.scrollIntoViewIfNeeded().catch(() => {});
> 21 |     await expect(footerNav).toBeVisible({ timeout: 20_000 });
     |                             ^ Error: expect(locator).toBeVisible() failed
  22 | 
  23 |     // Watch column (footerNav.ts FOOTER_WATCH)
  24 |     await expect(footerNav.getByRole('link', { name: 'Schedule' })).toBeVisible();
  25 |     await expect(footerNav.getByRole('link', { name: 'On Demand' })).toBeVisible();
  26 | 
  27 |     // Support column (FOOTER_SUPPORT)
  28 |     await expect(footerNav.getByRole('link', { name: 'FAQ' })).toBeVisible();
  29 |     await expect(footerNav.getByRole('link', { name: 'Contact' })).toBeVisible();
  30 | 
  31 |     // Legal bar
  32 |     const legal = page.getByRole('navigation', { name: 'Legal' });
  33 |     await expect(legal.getByRole('link', { name: /terms of use/i })).toBeVisible();
  34 |     await expect(legal.getByRole('link', { name: /privacy policy/i })).toBeVisible();
  35 |   });
  36 | });
  37 | 
```