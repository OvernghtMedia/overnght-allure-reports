# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: ../api/me-clip-cutter.spec.ts >> Me DTO — clipCutterAccess >> GET /me exposes a boolean clipCutterAccess (admin)
- Location: api/me-clip-cutter.spec.ts:13:9

# Error details

```
Error: {"statusCode":500,"message":"Internal server error"}

expect(received).toBeTruthy()

Received: false
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | import { getAuthHeaders, getUnauthenticatedHeaders } from '../utils/auth';
  3  | 
  4  | /**
  5  |  * TC-ME-CLIP-* — `clipCutterAccess` on the Me DTO (server d0c98d1
  6  |  * "expose clipCutterAccess on Me DTO"). `GET /me` now returns a boolean
  7  |  * `clipCutterAccess` (default false) that gates the clip-cutter tool.
  8  |  *
  9  |  * Requires ADMIN_TOKEN + REGULAR_TOKEN.
  10 |  */
  11 | test.describe('Me DTO — clipCutterAccess', { tag: '@smoke' }, () => {
  12 |   for (const role of ['admin', 'regular'] as const) {
  13 |     test(`GET /me exposes a boolean clipCutterAccess (${role})`, async ({
  14 |       request,
  15 |     }) => {
  16 |       const res = await request.get('/me', { headers: getAuthHeaders(role) });
> 17 |       expect(res.ok(), await res.text()).toBeTruthy();
     |                                          ^ Error: {"statusCode":500,"message":"Internal server error"}
  18 |       const body = (await res.json()) as Record<string, unknown>;
  19 |       expect(body, 'Me response must carry clipCutterAccess').toHaveProperty(
  20 |         'clipCutterAccess',
  21 |       );
  22 |       expect(typeof body.clipCutterAccess).toBe('boolean');
  23 |     });
  24 |   }
  25 | 
  26 |   test('GET /me without auth → 401', async ({ request }) => {
  27 |     const res = await request.get('/me', { headers: getUnauthenticatedHeaders() });
  28 |     expect(res.status()).toBe(401);
  29 |   });
  30 | });
  31 | 
```